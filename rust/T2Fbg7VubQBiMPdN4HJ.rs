// this file cannot be executed directly.

fn main() {
    let cnt = 146;
    let message = String::from("T2Fbg7VubQBiMPdN4HJ");
    for x in (0..cnt).rev() {
        println!("{}", message);
    }
}
