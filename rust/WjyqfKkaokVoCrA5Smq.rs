// this file cannot be executed directly.

fn main() {
    let cnt = 173;
    let message = String::from("WjyqfKkaokVoCrA5Smq");
    for x in (0..cnt).rev() {
        println!("{}", message);
    }
}
