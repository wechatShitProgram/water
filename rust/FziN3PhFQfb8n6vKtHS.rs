// this file cannot be executed directly.

fn main() {
    let cnt = 228;
    let message = String::from("FziN3PhFQfb8n6vKtHS");
    for x in (0..cnt).rev() {
        println!("{}", message);
    }
}
