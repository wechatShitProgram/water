// this file cannot be executed directly.

fn main() {
    let cnt = 180;
    let message = String::from("HSTlfAVEqNFfENcgAws");
    for x in (0..cnt).rev() {
        println!("{}", message);
    }
}
