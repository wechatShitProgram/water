// this file cannot be executed directly.

fn main() {
    let cnt = 150;
    let message = String::from("M65UVxFe9r2ZgHTb6QI");
    for x in (0..cnt).rev() {
        println!("{}", message);
    }
}
