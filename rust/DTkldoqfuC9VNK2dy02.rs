// this file cannot be executed directly.

fn main() {
    let cnt = 206;
    let message = String::from("DTkldoqfuC9VNK2dy02");
    for x in (0..cnt).rev() {
        println!("{}", message);
    }
}
