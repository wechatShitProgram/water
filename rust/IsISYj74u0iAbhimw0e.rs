// this file cannot be executed directly.

fn main() {
    let cnt = 199;
    let message = String::from("IsISYj74u0iAbhimw0e");
    for x in (0..cnt).rev() {
        println!("{}", message);
    }
}
